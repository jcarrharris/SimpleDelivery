<h1><%= @location.location_name %> </h1>


<div class="section-content">
    <div id="locationMap" style="width:500px;height:380px;"></div>
  </br>
  <div class="btn-group">
    <button type="button" class="btn btn-primary">Modify</button>
    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
      <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" role="menu">
        <li><%= link_to "Edit", edit_business_location_path(@business, @location) %></li>
        <li><%= link_to "Delete", [@business, @location], :confirm => "Are you sure you want to delete this location?", method: :delete %> </li>
      </ul>
    </br></br>
      <%= link_to "Create new order", new_business_location_order_path(@business, @location) %>
    </div>
  </br>
<table class="table-striped">
  <tbody>
  <tr><td>Location: <td class="value"><%= @location.location_name %></td>
  <tr><td>Address: <td class="value"><%= @location.address %></td>
  <tr><td>Coordinates:<%= Geocoder.coordinates(@location.address) %></td>
  <tr><td>Phone Number:<td class="value"> <%= @location.phone_number %></td>
  </tbody>
  </table>
    </div>

<div class="location_map">
 
</div>