<div class="section-content">
  <h1>Locations</h1>
  <div>
<% @location.each do |l| %>
<%= link_to l.location_name, business_location_path(l.business, l) %></div>
<button type="button" class="accord"data-toggle="collapse" data-target=".table-responsive">
  ^
</button>
    <div class="table-responsive">
      <table class="table table-hover">
      <tbody>
        <tr><td>Address:</td><td class="value"> <%= l.address %></td></tr>
        <tr><td>Phone Number:</td><td class="value"> <%= l.phone_number %></td></tr>
        <% end %>
      </tbody>
      </table>
    </div>
    </div>
  </div>
</div>