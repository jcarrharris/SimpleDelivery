$('#user_star').raty({
  score: <%= @rating.rate %>,
  path: '/assets/',
  click: function(score, evt) {
    $.ajax({
      url: '/users/' + <%= @user.id %> + '/ratings',
      type: 'POST',
      data: { score: score }
    }).done(function (data){});
  }
});